#include <iostream>
#include <string>
#include <iomanip>
#include <sstream>
#include <vector>
#include <fstream>
using namespace std;

int main(){
  
  string Pavarde, Vardas, ND1, ND2, ND3, ND4, ND5;
  vector<string>PV;
  vector<double>NDp;
  vector<double>Gp;
  
  //txt dokumentas
  string file;
  file = 'Studentai.txt';
  
  //eiluciu skaicius
  int i = 0;
  
  ifstream coedd(file);
  if (coeff.is_open())
  {
    string line;
    getline(coeff, line);
    
    while(!coeff.eof())
    {
      getline(coeff, Pavarde)
    }
    
  }

}
